# --- core (demo + authenticated area) ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
anthropic>=0.39
websockets>=13

# --- authenticated product area: accounts, persistence, sessions ---
SQLAlchemy>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
passlib[bcrypt]>=1.7
bcrypt>=4.0,<4.1          # 4.1 drops bcrypt.__about__, which passlib 1.7.x still reads
PyJWT>=2.8
email-validator>=2.0
python-multipart>=0.0.9

# --- synthetic monitoring (Phase 2) ---
httpx>=0.27

# --- notifiers (Phase 4) ---
resend>=2.0
